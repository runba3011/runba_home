<%= form_with(model: request_email, local: true) do |form| %>
  <% if request_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(request_email.errors.count, "error") %> prohibited this request_email from being saved:</h2>

      <ul>
        <% request_email.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
